import { createTheme } from "@mui/material/styles";
//mui is library Material UI (react components)

//allows customisation of appearance of components through defining a theme
export const theme = createTheme({
    palette: {
        mode: "dark",
    },

})